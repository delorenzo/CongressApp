<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    tools:context="com.jdelorenzo.congressapp.main.MainActivity">

    <android.support.design.widget.TabLayout
        android:background="@android:color/white"
        android:id="@+id/tab_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <android.support.design.widget.TabItem
            android:id="@+id/tab_local"
            android:text="@string/tab_local"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
        <android.support.design.widget.TabItem
            android:id="@+id/tab_favorites"
            android:text="@string/tab_all"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </android.support.design.widget.TabLayout>

    <ProgressBar
        android:layout_centerInParent="true"
        android:gravity="center_horizontal"
        android:id="@+id/progress_bar"
        android:layout_width="@dimen/progress_bar_size"
        android:layout_height="@dimen/progress_bar_size" />

    <android.support.v7.widget.RecyclerView
        android:layout_below="@+id/tab_layout"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:id="@+id/legislator_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/legislator_item" />

    <TextView
        android:text="@string/empty_text"
        android:layout_below="@id/tab_layout"
        android:gravity="center"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:id="@+id/legislator_list_empty_view"
        android:visibility="gone"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</RelativeLayout>
